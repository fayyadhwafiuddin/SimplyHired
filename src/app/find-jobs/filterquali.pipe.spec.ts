import { FilterqualiPipe } from './filterquali.pipe';

describe('FilterqualiPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterqualiPipe();
    expect(pipe).toBeTruthy();
  });
});
